import { morajlabBitMorajDesignSystemGlobals } from './morajlab.bit-moraj-design-system-globals';

describe('morajlabBitMorajDesignSystemGlobals', () => {
  it('should work', () => {
    expect(morajlabBitMorajDesignSystemGlobals()).toEqual(
      'morajlab.bit-moraj-design-system-globals'
    );
  });
});
