import { morajlabBitMorajDesignSystemVanilla } from './morajlab.bit-moraj-design-system-vanilla';

describe('morajlabBitMorajDesignSystemVanilla', () => {
  it('should work', () => {
    expect(morajlabBitMorajDesignSystemVanilla()).toEqual(
      'morajlab.bit-moraj-design-system-vanilla'
    );
  });
});
