import { uwpJsSrcLibAcrylic } from './uwp.js-src-lib-acrylic';

describe('uwpJsSrcLibAcrylic', () => {
  it('should work', () => {
    expect(uwpJsSrcLibAcrylic()).toEqual('uwp.js-src-lib-acrylic');
  });
});
